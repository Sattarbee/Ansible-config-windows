<!DOCTYPE html>
<html>
<head>
  <title>Ansible Windows Patching Report</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 2rem;
      background: #fff;
      color: #333;
    }
    h1 {
      text-align: center;
      margin-bottom: 1.5rem;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 2rem;
      font-size: 14px;
    }
    th, td {
      border: 1px solid #ccc;
      padding: 0.5rem 0.75rem;
      vertical-align: top;
      text-align: left;
    }
    th {
      background-color: #f0f0f0;
      font-weight: bold;
    }
    ul {
      margin: 0;
      padding-left: 20px;
    }
    .footer {
      text-align: center;
      font-size: 0.85rem;
      color: #666;
    }
  </style>
</head>
<body>
  <h1>Ansible Windows Patching Report</h1>
  <table>
    <thead>
      <tr>
        <th>Hostname</th>
        <th>Operating System</th>
        <th>Required Updates</th>
        <th>Last Patch Date</th>
        <th>Last Applied Patches</th>
        <th>KB Updates</th>
      </tr>
    </thead>
    <tbody>
      {% for windows_host in ansible_play_hosts | sort %}
      <tr>
        <td>{{ hostvars[windows_host]['inventory_hostname'] | default(windows_host) }}</td>
        <td>{{ hostvars[windows_host]['ansible_distribution'] | default('N/A') }} {{ hostvars[windows_host]['ansible_distribution_version'] | default('') }}</td>

        <!-- Required Updates -->
        <td>
          {% if required_updates_dict is defined and required_updates_dict[windows_host] is defined and required_updates_dict[windows_host] %}
            <ul>
              {% for patch in required_updates_dict[windows_host] %}
              <li>{{ patch.title | default(patch.name) }}</li>
              {% endfor %}
            </ul>
          {% else %}
            No updates required.
          {% endif %}
        </td>

        <!-- Last Patch Date -->
        <td>
          {% if last_applied_patches_dict is defined and last_applied_patches_dict[windows_host] is defined and last_applied_patches_dict[windows_host] %}
            {% set last_patch = (last_applied_patches_dict[windows_host] | sort(attribute='installed_date') | last) %}
            {{ last_patch.installed_date | default("N/A") }}
          {% else %}
            N/A
          {% endif %}
        </td>

        <!-- Last Applied Patches -->
        <td>
          {% if last_applied_patches_dict is defined and last_applied_patches_dict[windows_host] is defined and last_applied_patches_dict[windows_host] %}
            <ul>
              {% for patch in last_applied_patches_dict[windows_host] %}
              <li>
                {{ patch.name }}{% if patch.installed_date %} (Installed: {{ patch.installed_date }}){% endif %}
              </li>
              {% endfor %}
            </ul>
          {% else %}
            No patches applied last time.
          {% endif %}
        </td>

        <!-- KB Updates -->
        <td>
          {% set kb_list = [] %}
          {% if required_updates_dict is defined and required_updates_dict[windows_host] is defined %}
            {% for patch in required_updates_dict[windows_host] %}
              {% if patch.kb and patch.kb != 'N/A' %}
                {% set _ = kb_list.append(patch.kb) %}
              {% endif %}
            {% endfor %}
          {% endif %}
          {% if last_applied_patches_dict is defined and last_applied_patches_dict[windows_host] is defined %}
            {% for patch in last_applied_patches_dict[windows_host] %}
              {% if patch.kb and patch.kb != 'N/A' and patch.kb not in kb_list %}
                {% set _ = kb_list.append(patch.kb) %}
              {% endif %}
            {% endfor %}
          {% endif %}
          {% if kb_list %}
            <ul>
              {% for kb in kb_list %}
              <li>{{ kb }}</li>
              {% endfor %}
            </ul>
          {% else %}
            No KB data available.
          {% endif %}
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
  <div class="footer">
    Created with Ansible on {{ ansible_date_time.iso8601 | default("Unknown date") }}.
  </div>
</body>
</html>
