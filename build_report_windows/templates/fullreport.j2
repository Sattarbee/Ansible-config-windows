<!DOCTYPE html>
<html>
<head>
  <title>Ansible Windows Patching Report</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 2rem;
      background: #fff;
      color: #333;
    }
    h1 {
      text-align: center;
      margin-bottom: 1.5rem;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 2rem;
      font-size: 14px;
    }
    th, td {
      border: 1px solid #ccc;
      padding: 0.5rem 0.75rem;
      vertical-align: top;
      text-align: left;
    }
    th {
      background-color: #f0f0f0;
      font-weight: bold;
    }
    ul {
      margin: 0;
      padding-left: 20px;
    }
    .footer {
      text-align: center;
      font-size: 0.85rem;
      color: #666;
    }
  </style>
</head>
<body>
  <h1>Ansible Windows Patching Report</h1>
  <table>
    <thead>
      <tr>
        <th>Hostname</th>
        <th>Operating System</th>
        <th>Required Updates</th>
        <th>Last Applied Patches</th>
      </tr>
    </thead>
    <tbody>
      {% for windows_host in ansible_play_hosts | sort %}
      <tr>
        <td>{{ hostvars[windows_host]['inventory_hostname'] | default(windows_host) }}</td>
        <td>{{ hostvars[windows_host]['ansible_distribution'] | default('N/A') }} {{ hostvars[windows_host]['ansible_distribution_version'] | default('') }}</td>
        <td>
          {% if required_updates is defined and required_updates[windows_host] is defined and required_updates[windows_host] %}
            <ul>
              {% for patch in required_updates[windows_host] %}
              <li>
                {{ patch.name }}{% if patch.installed_date %} (Installed: {{ patch.installed_date }}){% endif %}
              </li>
              {% endfor %}
            </ul>
          {% else %}
            No updates required.
          {% endif %}
        </td>
        <td>
          {% if last_applied_patches is defined and last_applied_patches[windows_host] is defined and last_applied_patches[windows_host] %}
            <ul>
              {% for patch in last_applied_patches[windows_host] %}
              <li>
                {{ patch.name }}{% if patch.installed_date %} (Installed: {{ patch.installed_date }}){% endif %}
              </li>
              {% endfor %}
            </ul>
          {% else %}
            No patches applied last time.
          {% endif %}
        </td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
  <div class="footer">
    Created with Ansible on {{ ansible_date_time.iso8601 | default("Unknown date") }}.
  </div>
</body>
</html>
